var DFE;
(function (dfe) {
    (function (dqt) {
        (function (webResources) {
            (function (initialteachertraining) {
                initialteachertraining.Form_OnLoad = function (executionContext) {
                    if (executionContext != null) {
                        var formContext = executionContext.getFormContext();
                        if (formContext.ui.getFormType() == 1/*Create Form*/) {
                            formContext.getAttribute('dfeta_requestedbyid').setValue(null);
                        }
                    }
                }
            })(webResources.InitialTeacherTraining = webResources.InitialTeacherTraining || {});
        })(dqt.WebResources = dqt.WebResources || {});
    })(dfe.Dqt = dfe.Dqt || {});
})(DFE = DFE || {});